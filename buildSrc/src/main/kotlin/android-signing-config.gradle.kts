// load in keystore values if available otherwise load in debug values
loadExtra("keystorePassword", "android")
loadExtra("aliasKeyPassword", "android")
loadExtra("storeKeyAlias", "android")
loadExtra("keystoreLocation", "keys/debug.keystore")
